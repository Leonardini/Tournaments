# Which claims this experiment node runs.
#
# ONE line, space separated. Every node in the tree runs the identical command
# (`bash repro2609/run.sh`); what differs between nodes is THIS file and nothing
# else, so results stay comparable across the tree.
#
# available ids (paper cost in core-hours, from Appendix A.3/A.4):
#   build    compile kinduce at the pinned upstream commit + node-for-node regression gate
#   pos      sub-minute positive controls: P19 SAT, P23-v SAT, P23 arc-critical witness
#   calib    100-base-state slice of P23, to measure seconds/live-base-state
#   p23      B1  Paley(23) is not 5-inducible  -> N(5) <= 23        34.03 core-h
#   p31      P2  Paley(31) is not 5-inducible                      26.89 core-h
#   p19m1    P4  Paley(19) is not 5-inducible at unit margin         2.5 core-h
#   dr19     K9  2nd doubly regular tournament on 19 vertices        3.59 core-h
#   root19   B2  ROOT (CNF) portable hash, Paley(19) certification  minutes, no solving
#   root23   B2  ROOT (CNF) portable hash, Paley(23) certification  minutes, no solving

CLAIMS="root19 root23"
